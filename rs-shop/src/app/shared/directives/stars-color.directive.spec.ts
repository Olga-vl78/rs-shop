import { StarsColorDirective } from './stars-color.directive';

describe('StarsColorDirective', () => {
  it('should create an instance', () => {
    const directive = new StarsColorDirective();
    expect(directive).toBeTruthy();
  });
});
